const Tree = require('./binary-search-tree');

function knightMoves(start, end, count) {
  if (!start) return
   
}

// axis will be [x, y]
// cant move off board

const possibleMoves = [
  [1, 2],
  [2, 1],
  [2, -1],
  [1, -2],
  [-1, -2],
  [-2, -1],
  [-2, 1],
  [-1, 2],
];
